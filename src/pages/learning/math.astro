---
import "@styles/learning-pages.css";
import PageHeader from "@components/features/page-header/PageHeader.astro";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getCollection } from "astro:content";
import { removeFileExtension } from "@utils/url-utils";

const title = "考研数学";
const description = "备战考研数学的学习记录";

// 获取所有帖子并筛选分类为 'math' 的
const allPosts = await getCollection("posts");
const mathPosts = allPosts
	.filter((post) => post.data.category === "math" && !post.data.draft)
	.sort((a, b) => new Date(b.data.published).getTime() - new Date(a.data.published).getTime());
---

<MainGridLayout title={title} description={description}>
	<div class="learning-container">
		<div class="learning-content-base">
			<PageHeader title="考研数学" subtitle="记录数学备考的学习过程" />

			{mathPosts.length > 0 ? (
				<div>
					{mathPosts.map((post) => (
						<a href={`/posts/${removeFileExtension(post.id)}/`} class="learning-post-item block">
							<h3 class="learning-post-title text-blue-600 dark:text-blue-300">
								{post.data.title}
							</h3>
							<p class="learning-post-description">
								{post.data.description}
							</p>
							<div class="learning-post-meta">
								<span>{new Date(post.data.published).toLocaleDateString('zh-CN')}</span>
								{post.data.tags.length > 0 && (
									<div class="flex gap-2">
										{post.data.tags.slice(0, 3).map((tag) => (
											<span class="bg-blue-100 dark:bg-blue-900 px-2 py-1 rounded text-xs">
												{tag}
											</span>
										))}
									</div>
								)}
							</div>
						</a>
					))}
				</div>
			) : (
				<div class="learning-empty-state">
					<p class="learning-empty-message">
						📚 开始记录你的数学学习之旅...
					</p>
				</div>
			)}
		</div>
	</div>
</MainGridLayout>
